# Variáveis de ambiente locais — NÃO commitar em produção
# Copie este arquivo para .env: cp .env.example .env
# Os valores abaixo são os padrões para desenvolvimento local e funcionam sem alteração.

DB_USER=lugand
DB_PASSWORD=lugand
DB_HOST=localhost
DB_PORT=5432
DB_NAME=ui-agenda
CORS_ORIGINS=http://localhost:5173
